# dependencies
node_modules/
.pnpm-store/
__pycache__/
*.py[cod]
.venv/
venv/

# build output
.next/
# โฟลเดอร์ build ชั่วคราวตอนทดสอบขณะ dev server รันอยู่ (NEXT_DIST_DIR)
.next-verify/
out/
dist/
build/
*.egg-info/

# env & secrets
.env
.env.*
!.env.example

# เอกสารวางแผนภายใน — เป็นบันทึกระหว่างทางของทีมพัฒนา ไม่ใช่เอกสารของระบบ
# เก็บไว้ในเครื่องได้ แต่ไม่ต้องส่งมอบไปกับ repo
docs/plan-*.md
docs/deploy-environments.md

# data & uploads (ห้าม commit ไฟล์เกียรติบัตรจริงเด็ดขาด - เป็นข้อมูลส่วนบุคคล)
data/
uploads/
tmp/
*.pdf
*.xlsx
*.xls
!apps/worker/tests/fixtures/*.pdf
!apps/worker/tests/fixtures/*.xlsx

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
coverage/
.turbo/
playwright-report/
test-results/

# os / editor
.DS_Store
.idea/
*.swp

# build cache ของ TypeScript — เปลี่ยนทุกครั้งที่ build ไม่ควรเข้า git
*.tsbuildinfo
